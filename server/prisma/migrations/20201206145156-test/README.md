# Migration `20201206145156-test`

This migration has been generated by nickoferrall at 12/6/2020, 2:51:57 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    PRIMARY KEY ("id")
)

CREATE TABLE "Poll" (
    "id" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "userId" TEXT NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

CREATE TABLE "Option" (
    "id" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    "pollId" TEXT NOT NULL,

    FOREIGN KEY ("pollId") REFERENCES "Poll"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

CREATE TABLE "Vote" (
    "id" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "pollId" TEXT NOT NULL,
    "optionId" TEXT NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("pollId") REFERENCES "Poll"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("optionId") REFERENCES "Option"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201206145156-test
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,49 @@
+// 1
+datasource db {
+    provider = "sqlite"
+    url = "***"
+}
+
+// 2
+generator client {
+    provider = "prisma-client-js"
+}
+
+// 3
+model User {
+    id    String @id @default(cuid())
+    name  String
+    polls Poll[]
+    Vote  Vote[]
+}
+
+// 4
+model Poll {
+    id          String   @id @default(cuid())
+    description String
+    user        User     @relation(fields: [userId], references: [id])
+    options     Option[]
+    votes       Vote[]
+
+    userId String
+}
+
+// 5
+model Option {
+    id     String @id @default(cuid())
+    text   String
+    poll   Poll   @relation(fields: [pollId], references: [id])
+    votes  Vote[]
+    pollId String
+}
+
+// 6
+model Vote {
+    id       String @id @default(cuid())
+    user     User   @relation(fields: [userId], references: [id])
+    poll     Poll   @relation(fields: [pollId], references: [id])
+    option   Option @relation(fields: [optionId], references: [id])
+    userId   String
+    pollId   String
+    optionId String
+}
```


